@model IEnumerable<MTN.Models.Donhang>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}
<br />
<br />
<h2>THÔNG TIN ĐƠN HÀNG </h2>

<p>
    @Html.ActionLink("Thêm mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Đặt thanh toán :
        </th>
        <th>
           Tình trạng :
        </th>
        <th>
            Ngày đặt :
        </th>
        <th>
            Ngày giao:
        </th>
        <th>
            Mã người dùng :
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Datthanhtoan)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tinhtrang)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ngaydat)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ngaygiao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MaNguoidung)
        </td>
        <td>
            @Html.ActionLink("Sửa", "Edit", new { id=item.Madon }) |
            @Html.ActionLink("Chi tiết", "Details", new { id=item.Madon }) |
            @Html.ActionLink("Xóa", "Delete", new { id=item.Madon })
        </td>
    </tr>
}

</table>
